# Third-party sources fetched at setup time (see sim/README.md).
external/
# Build output (build/, build-asan/, ...).
build*/
# Rendered frames.
*.png
# ...except the committed golden thumbnails (sim/golden.py references).
!golden/*.png
