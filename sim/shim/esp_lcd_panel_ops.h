/* Host shim for esp_lcd_panel_ops.h -- opaque handle only (no panel on host). */
#ifndef SIM_ESP_LCD_PANEL_OPS_H
#define SIM_ESP_LCD_PANEL_OPS_H

typedef struct sim_esp_lcd_panel *esp_lcd_panel_handle_t;

#endif /* SIM_ESP_LCD_PANEL_OPS_H */
