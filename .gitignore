# ESP-IDF build output
build/
managed_components/
dependencies.lock
sdkconfig
sdkconfig.old

# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Personal, not shared
CLAUDE.local.md

# Secrets — never commit. Added before the remote existed, deliberately.
wifi.conf
secrets*
*.key
.env

# clangd index cache
.cache/

# Sim app state written by the store module at runtime
/state/
sim/state/
